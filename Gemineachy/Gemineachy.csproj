<Project Sdk="Microsoft.NET.Sdk.BlazorWebAssembly">

	<PropertyGroup>
		<TargetFramework>net10.0</TargetFramework>
		<Nullable>enable</Nullable>
		<ImplicitUsings>enable</ImplicitUsings>
		<!-- Disable compression (optional, based on use case) -->
		<CompressionEnabled>false</CompressionEnabled>
	</PropertyGroup>

	<ItemGroup>
		<!-- Direct reference so this WASM app gets the SpawnJS.WebWorkers build targets that produce
		     main.classic.js (the background service worker loads it via importScripts). -->
		<PackageReference Include="SpawnDev.SpawnJS.WebWorkers" Version="1.0.8" />
		<PackageReference Include="SpawnDev.SpawnJS" Version="1.1.14-local.1" />
		<PackageReference Include="SpawnDev.SpawnJS.BrowserExtension" Version="1.0.1" />
		<PackageReference Include="SpawnDev.SpawnJS.RazorUI" Version="1.0.4" />
		<!-- Direct ref for RazorRenderer 1.0.7 which exposes SpawnDomRenderer.OnAfterRenderAsync
		     (RazorUI pulls 1.0.6 transitively; the added event is additive/binary-compatible). -->
		<PackageReference Include="SpawnDev.SpawnJS.RazorRenderer" Version="1.0.7" />
	</ItemGroup>

</Project>
